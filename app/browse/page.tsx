import { BrowseClient } from "@/components/browse/BrowseClient";

export const metadata = {
  title: "Browse Vehicles - Motoxen",
};

export default function BrowsePage() {
  return <BrowseClient />;
}
